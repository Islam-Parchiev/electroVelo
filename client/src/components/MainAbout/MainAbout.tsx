import {FC} from 'react'

import styles from './MainAbout.module.scss';

const MainAbout:FC = () => {
	return (
		<section className={styles.MainAbout}>MainAbout</section>
	)
}

export default MainAbout
